#ifndef GLOBALS_H
#define GLOBALS_H

// global var used to tell toolbar and icons if the user is logged in as an admin
// will become true if admin is logged in then reset to false once logged out and wait
//for next login

extern bool isAdmin;

#endif // GLOBALS_H
